# ── Backend ───────────────────────────────────────────────────────────────────
backend/.env
backend/node_modules/
backend/uploads/*
!backend/uploads/.gitkeep

# ── Database — seed files ARE committed so Railway has initial data ───────────
# backend/data/*.json  ← intentionally NOT ignored

# ── OS & Editor ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.vscode/
*.log
